import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: "100%",
    paddingTop: 10,
    paddingLeft: 5,
    paddingRight: 5,
    backgroundColor: "#224A8A",
  },
});
