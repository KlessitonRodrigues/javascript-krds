import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: "100%",
    marginTop: 30,
    paddingLeft: 10,
    paddingRight: 10,
    backgroundColor: "#444",
  },
});
