import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    position: "absolute",
    backgroundColor: "#222",
    width: 48,
    height: 48,
    bottom: 16,
    right: 16,
    borderRadius: 24,
  },
  text: {
    color: "#FFF",
    fontSize: 36,
    textAlign: "center",
  },
});
