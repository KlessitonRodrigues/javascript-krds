import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 8,
        marginTop: 8,
        height: "15%",
    },
    note: {
        backgroundColor: "#FFF",
        borderRadius: 12,
        padding: 8,
        fontWeight: "bold",
        fontSize: 20,
        marginBottom: 8
    },
    badge: {
        color: "#224A8A",
        fontWeight: "bold",
        fontSize: 26,
    }
})