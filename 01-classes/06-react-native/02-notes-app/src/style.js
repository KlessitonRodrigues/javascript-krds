import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: "100%",
    paddingTop: 30,
    paddingLeft: 10,
    paddingRight: 10,
    backgroundColor: "#CBE0F6",
  },
});
