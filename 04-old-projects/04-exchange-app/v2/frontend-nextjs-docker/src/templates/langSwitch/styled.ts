import styled from 'styled-components'

export const Container = styled.div`
  border-radius: 6px;
  font-weight: bold;
  display: inline-flex;
  align-items: center;
  cursor: pointer;
  font-size: 0.9rem;
  padding: 0 0.25rem;
`
