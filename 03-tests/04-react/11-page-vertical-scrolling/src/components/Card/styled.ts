import styled, { css } from "styled-components";

export const Container = styled.div(
  (props) => css`
    padding: 4rem;
    background-color: #777;
    border-radius: 1rem;
    margin: 1rem;
  `
);
